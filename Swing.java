import java.io.*;
import javax.swing.*;
class SWING{
public static void main(String[] args)
    }
JFrame frame = new JFrame();
JButton button = new JButton("Click");
button.setBound(150,200,220,50);
frame.add(button);
frame.setSize(500,600);
frame.setLayout(null);
frame.setvisible(true);